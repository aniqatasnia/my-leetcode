class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        seen = defaultdict(int)
        for n in nums:
            if n in seen:
                return True
            else:
                seen[n] = n

        return False
